# Learn English

## Environment

1. Copy `.env.example` to `.env`.
2. Set `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` with your own project values.

Security note:
- Never commit `.env`.
- Use placeholders only in `.env.example`.
